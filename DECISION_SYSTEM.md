# Decision System adoption

IdeaForge usa il project id canonico `ideaforge`.

Le conversazioni autorizzate possono essere osservate dal bridge centrale e convertite in evidenze minimizzate. Il transcript grezzo non viene persistito nel repository.

Le evoluzioni IdeaForge sono `review_required` con autonomia massima 1. Il Decision System puo' produrre proposte e branch verificati, ma non integra automaticamente modifiche al sito o alla configurazione.

Il manifest autorevole locale e' `decision-system.project.json`; il registro canonico resta nel repository `francesco171717/Decision-System`.

Sono sempre esclusi credenziali, segreti, workflow e bypass delle protezioni GitHub.
